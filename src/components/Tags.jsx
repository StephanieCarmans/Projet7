import React from 'react';

const Tags = ({tags}) => {
      return (
            <div className="tagbtn">
             
                  <div className="">{tags}</div>
            </div>
      );
};

export default Tags;
